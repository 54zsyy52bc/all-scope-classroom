# 课堂管理系统 · 真机测试前全面自检结果清单

> 检查日期：2026-09-05 ｜ 检查对象：v4 三端（Preset Studio 办公端 / 教室教师端+大屏 / 学生机端）+ SIoT2 broker + 交付包
> 方式：全量代码走查（三端源码逐文件核对）+ 自动化测试重跑（含真实 SIoT broker 全链路冒烟）+ 交付包一致性核验 + 边界/异常场景静态审查
> 测试命令：`classroom-mgmt/run-tests.sh --full` → **14/14 全部通过**（含教师端 4 套、Preset Studio 2 套、学生端 3 套、质量门禁 2 项、真实 broker 冒烟 smoke 77/77 与 net.broker 11/11、broker/server 启动就绪）

---

## 一、检查范围与方法

| 层 | 覆盖内容 | 方式 |
|----|---------|------|
| 需求对齐 | FR-01~FR-13、AC-1~AC-8、v3 活动/计时/策略、v4 预设包、v4.2 分组/上课器材/单活动包 | 对照 01_SRS、07_状态流转、06_交付说明逐项核对代码 |
| 教师端 | server/src(19)/public(6)/db/storage/测试 | 全文走查 + smoke 实跑 |
| 学生端 | main/preload/renderer(8)/协议/测试 | 全文走查 + 真实 broker 联调实跑 |
| Preset Studio | electron/src/store/renderer(4)/测试 | 全文走查 + store/renderer DOM 测试实跑 |
| 共享契约 | topics.js / icons.js / preset-package.js（双端逐字一致已 diff 核验） | 代码比对 |
| 交付包 | 1_PresetStudio、2_教师机、3_学生端 目录与 zip；app-config、broker conf、bat | 文件级核验 + 修复后重打 |

---

## 二、已验证通过项（核对清单）

### A. 需求功能点（源码实存 + 测试通过）

| 需求 | 结论 | 证据 |
|------|------|------|
| FR-01 上课启动（广播+阶段→checkin） | ✅ 通过 | `session.service.startSession`；smoke step2 |
| FR-02 学生登记+必填校验+提交锁定 | ✅ 通过 | 学生端 `actions.submitCheckin` 校验/乐观置位；教师端幂等落库；protocol/renderer 测试 |
| FR-03 器材领取登记（预设动态下发清单） | ✅ 通过 | `cmd equipment` 广播 + hello→sync 补发；smoke "LED preset=5" 证据 |
| FR-04 实时大屏（≤1s 聚合） | ✅ 通过 | SSE revision 同源 + snapshot 重取；smoke 事件断言 |
| FR-05 发布任务/活动（预设/自定义） | ✅ 通过 | `POST /activities`（兼容 /tasks）；大屏双来源弹窗 |
| FR-06 任务三态反馈（≤1 步） | ✅ 通过 | 学生端三按钮单步上报；`task.status` SSE 聚合 |
| FR-07 小组墙/求助高亮闪烁/置顶 | ✅ 通过 | 大屏 `groups` 渲染 + `.s-help` 动画 + 组徽标；dashboard.dom 33/33（现 34） |
| FR-08 下课→归还阶段 | ✅ 通过 | `transitionPhase→return` + `cmd end` 广播 |
| FR-09 归还确认（逐项勾选→确认） | ✅ 通过 | 学生端归还页 + 服务端 `recordReturn`；无借用器材可一键直返 |
| FR-10 全部归还自动关机 + 强制/提醒 | ✅ 通过 | `autoShutdown` 全量判定 + 三重闸门（阶段/归还/HMAC60s 窗）；学生端 HMAC 校验 + dryRun 兜底；smoke 关机链路 |
| FR-11 一键导出 xlsx+4csv | ✅ 通过 | `export.service`（CSV 带 BOM）；大屏多文件下载；smoke 全 200 可下载 |
| FR-12 断线指数退避重连+补发关键状态 | ✅ 通过 | 两端 backoff 1→30s+jitter；hello→sync 带 equipment/currentTask/policy；smoke/net.broker 实测 |
| FR-13 连接状态常驻指示 | ✅ 通过 | 大屏/学生端常驻徽标；离线禁用提交并 toast |
| v3 活动计时器（权威计时+双方提醒+锁定联动） | ✅ 通过 | 暂停/继续/调时长/重计时/结束/到点提醒；策略引擎按 4 时机重算 |
| v3 锁定策略（activity 模式非活动期软锁） | ✅ 通过 | 全屏遮罩+心跳保持；smoke 锁定→发布解锁→停止重锁 闭环 |
| v4 预设包导入预演三态/提交/导出/删卫生 | ✅ 通过 | preset-pkg 23/23 + smoke 导出→导入→本地较新保护 |
| v4.2 分组配置/上课器材/字典互通/单活动包 | ✅ 通过 | store 36 + renderer 15；v4.2 端到端真跑记录在案 |
| 一键测试与质量门禁 | ✅ 通过 | 14/14；单文件≤300、无 emoji、无硬编码色、按钮≥80px |

### B. 用户操作主流程核验（一节课闭环，逐跳转检查）

1. 教师机 `start-teacher.bat` → SIoT 就绪(1883) → 教师服务就绪(3000) → 大屏自动打开 = ✅（bat 已含 node 检测与双服务启动）
2. 大屏「预设管理→导入 .kctpreset」：选文件→预演（新增/覆盖/跳过）→确认落库 → = ✅（两段式；坏包 E-PKG-01 提示）
3. 「开始上课」选班级/活动预设 → 座位按分组播种、器材合并去重下发、策略下发、SSE 就绪 = ✅（含 classPresetId→groups→totalSeats 联动）
4. 学生机登记（姓名/座号/器材勾选+数量）→ 提交即锁 → 大屏即时更新 = ✅
5. 发布活动（预设一键带出或自定义+计时）→ 阶段 task → 计时条出现 = ✅
6. 计时控制 暂停/继续/调时长/重计时/结束 + 到点双方提醒 = ✅（**修复后重挂定时器已回归验证**，见问题 1）
7. 锁定策略切换：讲解期锁屏遮罩、发布即解锁 = ✅
8. 下课 → 归还页 → 全部勾选归还 → 自动下发 HMAC 关机（dryRun 演练提示） = ✅
9. 结束课堂归档 → 可再开新班 = ✅（冲突记录已随新开课清零，见问题 2）
10. 导出 Excel/CSV → `exports/E-<id>/` 下载 = ✅

### C. 质量/安全护栏（走查确认）
- MQTT 消息逐条 try/catch，坏 JSON/缺 type/未知 type 均忽略不断流
- 事件 msg_id 幂等与业务写入同事务（防毒丸），SSE 广播在事务后
- 关机三重闸门 + HMAC 主进程终裁 + `timingSafeEqual` + 60s 时间窗 + 占位 secret 强制 dry-run
- 座位/文本输入双端净化（seat 1-99、长度裁剪）；渲染层不可信、IPC 白名单
- Express 统一信封与 BusinessError 映射；导出路径穿越白名单；SQL 参数化 + 列白名单
- better-sqlite3 不可用自动降级 JSON 存储（功能完整）
- 单实例锁（三应用）、配置写原子化（tmp+rename）、损坏数据文件自动 .bak 归档

---

## 三、发现的问题与修复情况

### 已修复（本次完成并回归通过）

| # | 级别 | 问题 | 修复 | 文件 |
|---|------|------|------|------|
| 1 | 高 | **活动计时器"暂停/继续/调时长/重计时"后未重挂截止定时器**：暂停后继续若越过原截止点→计时永不截止（无提醒、不锁屏）；调时长/重计时后→按旧截止点提前截止。smoke 的 6 秒用例恰因"暂停后旧定时器仍触发"掩盖了缺陷 | 状态迁移时同步管理定时器：pause/stop 摘除；resume/adjust/restart/start 以**新剩余时间重挂** | `teacher/src/services/activity.service.js` |
| 2 | 高 | **教师端提前"结束"计时活动不广播学生端**：open 策略下学生机仍显示一个本地继续倒数、永不消失的计时器（与 07 文档"stop→学生端移除计时器"不符） | stop 也广播 `task_timer{stop}`；学生端收到置 timerState=closed（视图收起计时器） | 同上 + `student/renderer/control.js` |
| 3 | 高 | **座位冲突记录跨会话永久累积、无法清除**：t_conflict 以 seat 为主键无 session 维度，hello/checkin 每节课累加 machineId；同一机器按手册"删 machineId 重新生成"后**旧 id 仍在→整节课误报红色冲突**；且无清除入口 | ①新开课 `startSession` 清空历史冲突 ②复位座位 `sendReset` 同步删除该座冲突行 ③新增 `clearConflicts/removeConflict` 仓储（SQLite+JSON 双后端同接口） | `db/index.js`、`session.service.js`、`command.service.js` |
| 4 | 高 | **错误码映射缺失**：`E-TIMER-01`（如重复暂停）与 `E-PKG-01`（坏预设包）未注册→回落 HTTP 500，前端误判"服务器故障" | 补注册：E-TIMER-01→409(40906)、E-PKG-01→400(40002) | `teacher/src/errors.js` |
| 5 | 中 | **座位越界无服务端防护**：学生机可上报 seat 51/越界（教师端直接落库），造成 t_student 幻影行、大屏"未登记/离线"变负、dashboard 统计错乱 | hello/checkin/task/return/status 全部加座位范围守卫（1-99 且 ≤ 会话 totalSeats），越界忽略不落库；totalSeats 未知的测试桩退化为 1-99 校验（兼容无头测试） | `teacher/src/mqtt/handlers.js` |
| 6 | 中 | **开课接口座位/分组无服务端上限**：API 直调可造超大会话（全量播种卡顿） | 服务端兜底：totalSeats 1-99、groupSize 1-20、自定义分组合计 1-99，越界 E-VAL-01 | `session.service.js` |
| 7 | 中 | **关键操作无防快速连点**：双击"开始上课/下课/关机/复位/发布活动/计时按钮/策略开关"会重复请求 | 大屏 app.js/activity.js 增加 per-action 请求期间防抖（busy guard）；复位按 seat 粒度 | `public/app.js`、`public/activity.js` |
| 8 | 中 | **学生端确认登记/确认归还可重复提交**（连点/回车），重复 checkin 会重复插入借还明细 | submitCheckin/submitReturn 前置 checkinDone/returnDone 防重；服务端本就 msg_id 幂等双保险 | `student/renderer/actions.js` |
| 9 | 低 | 大屏活动条"已截止"每次快照刷新重复弹 toast | 同一活动仅首次进入 expired 弹一次 | `public/activity.js` |
| 10 | 低 | dashboard 统计 `pending/offline` 在越界/超量上报时可负 | 与 #5 配套，改为 `Math.max(0,…)` 防御 | `dashboard.service.js` |
| 11 | **高（部署）** | **交付包学生端内置固定 `machineId:"M-d0d44063"`**：整包拷 50 台学生机时所有机器 MQTT clientId 相同→互相踢下线（与手册"留空自动生成"矛盾） | 已把 `3_学生端`（目录+zip）app-config 的 machineId 置空，首次启动按"主机名+MAC"自动生成唯一 id | `3_学生端/resources/app/app-config.json` |

**回归结果：修复后 `run-tests.sh --full` 全量重跑 14/14 通过**（协议/渲染/门禁/真实 broker smoke 无回归；smoke 计时用例在修复后依然在 20s 窗内收到截止提醒，验证新重挂逻辑正确）。

### 确认属既有设计/低优先（未改动，真机测试留意）

| 项 | 说明 | 建议 |
|----|------|------|
| 大屏当前活动条只跟踪"最近发布的活动" | 若教师不结束旧活动又发新任务，旧计时条被顶替（服务端仍计时）。设计上"一活动进行中"模型，正常用法不受影响 | 真机按"结束上一活动再发下一活动"操作即可 |
| 复位座位冲突若同座两台机同时在线上报会再次标红 | 这是功能不是缺陷（检测重复占座） | 复位后让其中一台换座号重登记 |
| 学生端器材勾选取消后再勾恢复为 1 而非预设量 | 预设=默认预填，用户改量后按实际数量走 stepper 即可 | 观察学生是否误操作；可后续优化"恢复预设"按钮 |
| Preset Studio 分组人数合计无 ≤99 的 UI 强约束 | 教师端导入校验会以"totalSeats 须 1-99"拒绝，属"自产自纠"，不会污染教室库 | 建议后续在 Studio 保存时即时提示 |
| 大屏 SSE 瞬断时连接灯短暂误标"SIoT 离线" | EventSource 自动重连，stream.ready 全量快照会立即纠正 | 观察即可；如介意再优化（断开只提示"大屏重连中"） |
| 教师端 `app.listen` 无 error 监听 | 端口被占时无友好提示（uncaughtException 兜底打日志） | 真机确保 3000 端口未被占用；或后续补监听提示 |
| 学生端同座两人先后用同一座号登记 | 后登记覆盖前一人数据但服务端标红该座→教师复位让双方重登记 | 已列真机重点项 5 |
| 教师端重启后进程内计时定时器丢失 | 学生端本地倒数到 0 会本地提示，重连 sync 按教师端真值校正 | 上课期间勿重启教师服务 |

---

## 四、真机测试建议重点关注项（按优先级）

**准备项**
1. 教师机装 Node 22 LTS；关闭 3000/1883 端口占用后双击 `start-teacher.bat`，浏览器自动开大屏见"待课"。
2. **先把教师机 `app-config.json` 的 `HMAC_SECRET` 换成随机串，并把同一值写到所有学生机 `app-config.json` 的 `secret`**（否则关机指令永远被学生端拒收——占位 secret 是安全特性）。
3. 每台学生机：`siotIp` 改为教师机局域网 IP；**第一节试用课保持 `dryRun:true`**；确认本机 `machineId` 为空（本次交付包已置空），用 `ipconfig` 查 IP、两机务必使用不同座号。

**功能重点（逐项做一遍）**
4. **计时器暂停→继续→调时长→重计时→结束**全按一次，重点：暂停几秒后继续、越过原截止点继续、调成更长/更短时长——确认大屏计时条与学生端倒计时都按新截止时间走，不再提前/永不截止（问题 1 修复点）。
5. 教师提前"结束"活动（open 策略下）→ 学生端计时器应**立即消失/收拢**，不再继续倒数（问题 2 修复点）。
6. 第二节课开课前确认：上一节曾出现过的"座位冲突"红色告警应**不再残留**（问题 3 修复点）。
7. 连续双击"开始上课/下课/强制关机/复位/发布活动"→ 只应产生一次请求，并提示"操作处理中"（问题 7）。
8. 学生端连点两次"确认登记/确认归还"→ 只登记/归还一次，借还台账无重复数量（问题 8）。
9. 两台学生机故意填同一座号 → 大屏该座红色虚线冲突提示；教师点该座复位 → 两台都回到登记页且红色解除。
10. 关机演练：全部归还后自动弹"关机演练/将关机倒计时"提示条；把 `dryRun` 改 `false` 前先在**一台无关紧要的机器**验证真关机。
11. 断网演练：拔网线 10 秒→学生端"重连中"→插回→自动恢复并补齐状态；大屏学生在线/离线灯按 15s/45s 阈值变化。
12. 导出：`导出 Excel` 生成 `课堂报表.xlsx`（4 sheet）+ CSV（带 BOM，Excel 打开中文不乱码），与 2 人实登数据一致。

**50 台并发参考**
13. 心跳 15s/判离线 45s、重连退避 1→30s+jitter 已按架构实现，但 **50 台并发压测尚未执行**——真机若 ≥20 台建议先在完整机房做一次"同时开课→全班 1 分钟内登记"冒烟，观察大屏 1 秒内刷新与无掉线。

**教师服务进程注意**
14. 上课期间**不要重启教师服务**（进程内计时定时器会丢失，学生端以教师端 sync 为真值校正；下课归档后再重启最稳妥）。
